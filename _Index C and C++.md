--------------------------------------------------------------------------------

На мой взгляд, самым началом данного конспекта должен являться именно АрхЭВМ. Как никак, в соответствии с курсом — надо знать, как устроены некоторые аспекты памяти, хранения переменных и т.д.

--------------------------------------------------------------------------------
## АрхЭВМ

- **[[ArcEvm#Байт|Байт]]** — минимальная адресуемая единица памяти и связь байта с битами.
- **[[ArcEvm#Кодирование со старшим битом под знак|Кодирование со старшим битом под знак]]** — исторический способ представления знаковых чисел.
- **[[ArcEvm#Кодирование со смещением|Кодирование со смещением]]** — представление со сдвигом.
- **[[ArcEvm#Дополнение до двух (Two's Complement)|Дополнение до двух]]** — основной современный способ кодирования знаковых целых.
- **[[ArcEvm#Числа с фиксированной точкой (Fixed Point)|Числа с фиксированной точкой]]** — представление дробей с фиксированным положением точки.
- **[[ArcEvm#Числа с плавающей точкой (Floating Point)|Числа с плавающей точкой]]** — мантисса, экспонента, IEEE-754 и нестандартные форматы.
- **[[ArcEvm#Half Precision и специальные значения|Half precision и специальные значения]]** — денормализованные числа, `inf`, `NaN`.
- **[[ArcEvm#Методы округления|Методы округления]]** — округление к ближайшему и tie-to-even.

--------------------------------------------------------------------------------

Основной раздел по языку C. Здесь собраны заметки о представлении данных, типах, памяти, конструкциях языка, функциях стандартной библиотеки и технических деталях.

--------------------------------------------------------------------------------
### Выполнение программы

- **[[Build#Препроцессор (Preprocessing)|Препроцессор]]** — роль директив `#`.
- **[[Build#Компиляция (Compilation)|Компиляция]]** — перевод исходников в объектные файлы.
- **[[Build#Линковка / Компоновка (Linking)|Линковка]]** — сборка исполняемого файла.
- **[[Build#Запуск и выполнение|Запуск программы]]** — где реально начинается программа.
- **[[Build#Undefined Behaviour (UB)|Undefined Behaviour]]** — случаи, в которых стандарт снимает гарантии.

### Базовые типы

- **[[Data Types#Базовые типы|Базовые типы]]** — `int`, `void` и базовая роль типа в языке.
- **[[Data Types#Дополнительные целочисленные типы|Дополнительные целочисленные типы]]** — `short`, `long`, `long long`, `char`, `_Bool`, фиксированные размеры и `_BitInt(N)`.
- **[[Data Types#Типы с плавающей точкой|Типы с плавающей точкой]]** — `float`, `double`, `long double`, `_Float16`, комплексные числа и связь с IEEE-754.
- **[[Data Types#Спецификаторы форматирования|Спецификаторы форматирования]]** — `%i`, `%u`, `%lli`, `%lld`, `%llx` и соглашения ввода-вывода.

### Бинарные операции

- **[[Arithmetic operations#Обычные операции|Обычные операции]]** — `+`, `-`, `*`, `/`, `%`, приведения и тонкости деления.
- **[[Arithmetic operations#Побитовые операции|Побитовые операции]]** — `&`, `|`, `^`, `~`, `<<`, `>>`.
- **[[Arithmetic operations#Логические операции|Логические операции]]** — `&&`, `||`, `!` и неполное вычисление.
- **[[Arithmetic operations#Инкременты|Инкременты и декременты]]** — `i++`, `++i`, `i--`, `--i` и их различия.

###  Функции

- **[[Function Reference#Анатомия функции и прототипы|Анатомия и прототипы]]** — объявление, прототипы и пофайловая компиляция.
- **[[Function Reference#Некоторые стандартные функции|Навигатор стандартных функций]]** — самые частые стандартные функции, начиная от `printf` и заканчивая `strcmp`.

### Базовые конструкции

- **[[C language constructs#if|if]]** — ветвление.
- **[[C language constructs#Циклы: while, do while, for|Циклы]]** — `while`, `do while`, `for`.
- **[[C language constructs#switch|switch]]** — множественный выбор и `case`.
- **[[C language constructs#goto и метки|goto]]** — метки и переходы.

### Данные

- **[[Variables and structures#Область видимости и инициализация|Область видимости]]** — локальные и глобальные объекты, стек, секция данных и инициализация.
- **[[Variables and structures#Структуры (struct)|Структуры]]** — объявление `struct`, поля, создание объектов и `typedef`.
- **[[Variables and structures#Выравнивание и компоновка структур|Выравнивание структур]]** — padding и связь расположения полей с производительностью и ABI.
- **[[Variables and structures#Ключевые слова и модификаторы|Ключевые слова]]** — `static`, `enum`, `const`, `restrict`, `constexpr`.

### Указатели

- **[[Pointers and arrays#Что такое указатель|Указатели]]** — адреса, разыменование, `NULL`.
- [[Pointers and arrays#Арифметика указателей|Арифметика указателей]] — почему `10 + 1` необязательно `11`.
- **[[Pointers and arrays#Массивы|Массивы]]** — преобразование массива к указателю и сходство и разница с указателем.
- [[Pointers and arrays#Многомерные массивы|Многомерные массивы]] — хранение в памяти, обязательные аргументы.
- [[Pointers and arrays#Строки|Подходы к хранению строки]] — как хранить строку в памяти.
- **[[Pointers and arrays#Строки в C|Строки в C]]** — нуль-терминированные строки и их связь с массивами `char`.

### Динамическая память

- [[Memory#Основные сегменты памяти|Основные сегменты памяти]] — стек, куча, секция данных и их различия.
- [[Memory#Динамическое выделение памяти|Динамическое выделение памяти в C]] — `malloc`, `calloc`, `realloc` и их различия.
- [[Memory#`free`|Освобождение динамической памяти]] — `free` и её суть.
- [[Memory#Ошибки при работе с динамической памятью|Ошибки при работе с динамической памятью]] — `Memory Leak`, `Double Free`, `Dangling Pointers` и спасение от них.

### Разное

 - **[[Other#Комментарии в C|Комментарии]]** — синтаксис комментариев, склейка строк и служебные возможности.
- **[[Other#__PRETTY_FUNCTION__|__PRETTY_FUNCTION__]]** — диагностика имени текущей функции.
- **[[Other#Endian-ность|Endian-ность]]** — little-endian и big-endian.

--------------------------------------------------------------------------------

Писать код без header-файлов — просто нереально, потому что в них можно писать прототипы функций для разделения кода на файлы, выносить константы, писать макросы, дефайны и др. Тем не менее, они относятся как к языку C, так и к C++, а сам конспект будет содержать уже смешанную информацию.

--------------------------------------------------------------------------------
## Header-files

- [[Header files#Назначение заголовочных файлов и директива `#include`|Заголовчные файлы]] — что это такое и что значит `#include`.
- [[Header files#Макросы и директива ` define`|Макросы]] — что такое макросы, `#define`.
- [[Header files#Директивы условной компиляции|Условная компиляция]] — использование условных операторов в компиляции.
- [[Header files#Защита от повторного включения (Include guards)|Include guards]] — секрет трёх строчек каждого заголовочного файла.
- [[Header files#Совместимость с C++ (`extern "C"`)|Совместимость C и C++]] — использование заголовочных файлов для связывания кода на C и C++.
- [[Header files#Встраивание ресурсов|Встраивание ресурсов]] — добавление внешних данных в программу.

--------------------------------------------------------------------------------
## CMake

- **[[CMake basics#Зачем нужен CMake|Зачем нужен CMake]]** — описание сборки проекта вместо ручных длинных команд компилятора.
- **[[CMake basics#Минимальный `CMakeLists.txt`|Минимальный CMakeLists.txt]]** — `cmake_minimum_required`, `project`, `add_executable`.
- **[[CMake basics#Как запустить сборку|Запуск сборки]]** — `cmake -S . -B build` и `cmake --build build`.
- **[[CMake basics#Несколько `.cpp`|Несколько cpp-файлов]]** — как указать несколько исходников.
- **[[CMake basics#Include-директории|Include-директории]]** — `target_include_directories`.
- **[[CMake basics#Библиотеки в CMake|Библиотеки]]** — `add_library` и `target_link_libraries`.
- **[[CMake basics#`PRIVATE`, `PUBLIC`, `INTERFACE`|PRIVATE/PUBLIC/INTERFACE]]** — что прокидывается пользователям цели.
- **[[CMake basics#Debug и Release|Debug и Release]]** — типы сборки.

--------------------------------------------------------------------------------
## C++

Раздел по C++ продолжает идеи из C, но добавляет новые механизмы языка: ссылки, методы, классы, конструкторы, деструкторы, `new/delete`, наследование, пространства имен, шаблоны, исключения и лямбды.

### Основы C++

- **[[C++ basics#`std::`|std::]]** — почему стандартные C++-имена часто пишутся с префиксом `std::`.
- **[[C++ basics#Аргументы по умолчанию|Аргументы по умолчанию]]** — значения параметров по умолчанию и правило "все правее тоже по умолчанию".
- **[[C++ basics#Передача по значению, указателю и ссылке|Передача данных]]** — передача по значению, указателю и ссылке.
- **[[C++ basics#Ссылки|Ссылки]]** — `int &`, привязка к объекту, отличие от указателя и висячие ссылки.
- **[[C++ basics#`nullptr`|nullptr]]** — нормальный нулевой указатель вместо `NULL`.
- **[[C++ basics#`const`-ссылки|const-ссылки]]** — передача больших объектов без копирования и продление жизни временных объектов.
- **[[C++ basics#Инициализация в C++|Инициализация]]** — `int x(2)`, `int x{2}` и most vexing parse.

### Классы, методы и операторы

- **[[C++ classes and operators#Методы структур|Методы структур]]** — функции внутри `struct`, скрытый `this` и определение метода через `S::f`.
- **[[C++ classes and operators#`static`-методы и `static`-поля|static-члены]]** — методы без `this` и поля, общие для всего типа.
- **[[C++ classes and operators#`public`, `private`, `protected`|public/private/protected]]** — управление видимостью и инкапсуляция.
- **[[C++ classes and operators#`struct` и `class`|struct и class]]** — почти одно и то же, но с разной видимостью по умолчанию.
- **[[C++ classes and operators#`const`-методы|const-методы]]** — методы, которые можно вызывать у `const`-объектов.
- **[[C++ classes and operators#`mutable`|mutable]]** — поле, которое можно менять даже внутри `const`-метода.
- **[[C++ classes and operators#Перегрузка функций|Перегрузка функций]]** — одинаковые имена функций при разных параметрах.
- **[[C++ classes and operators#Перегрузка операторов|Перегрузка операторов]]** — `operator+`, `operator+=`, префиксный и постфиксный `++`.
- **[[C++ classes and operators#`friend`|friend]]** — точечный доступ внешней функции к закрытым полям.

### Время жизни объектов

- **[[C++ lifetime#Конструкторы|Конструкторы]]** — создание объекта и отличие от простого выделения памяти.
- **[[C++ lifetime#Список инициализации|Список инициализации]]** — инициализация `const`-полей, ссылок и порядок полей.
- **[[C++ lifetime#`explicit`|explicit]]** — запрет нежелательных неявных преобразований.
- **[[C++ lifetime#Операторы преобразования типа|Операторы преобразования]]** — `operator int()` и опасность слишком неявного кода.
- **[[C++ lifetime#Деструкторы|Деструкторы]]** — действие перед смертью объекта.
- **[[C++ lifetime#RAII|RAII]]** — привязка ресурса к объекту.
- **[[C++ lifetime#Копирование объектов|Копирование объектов]]** — конструктор копирования и проблема поверхностной копии.
- **[[C++ lifetime#Оператор присваивания|Оператор присваивания]]** — присваивание уже существующему объекту и `this != &other`.
- **[[C++ lifetime#Правило трех|Правило трех]]** — деструктор, конструктор копирования и оператор присваивания.
- **[[C++ lifetime#`= delete`|delete/default]]** — удаление или явная генерация специальных функций.

### Динамическая память в C++

- **[[C++ dynamic memory#Обычное создание объекта и динамическое создание|Обычное и динамическое создание]]** — чем `S s{...}` отличается от `new S{...}`.
- **[[C++ dynamic memory#`new` и `delete` для простого типа|new/delete для простого типа]]** — динамическое создание одного объекта и ручное удаление.
- **[[C++ dynamic memory#`new` и `delete` для структур|new/delete для структур]]** — выделение памяти вместе с вызовом конструктора и деструктора.
- **[[C++ dynamic memory#Скалярный и массивный `new`|new[] и delete[]]]** — почему нельзя путать массивную и скалярную формы.
- **[[C++ dynamic memory#`new` и неудачное выделение памяти|bad_alloc и nothrow]]** — что происходит, если памяти не хватило.
- **[[C++ dynamic memory#Placement new|Placement new]]** — создание объекта в уже выделенной памяти.
- **[[C++ dynamic memory#Смешивать `malloc/free` и `new/delete` нельзя|malloc/free и new/delete]]** — почему пары выделения и освобождения нельзя перемешивать.

### Наследование

- **[[C++ inheritance#Базовый и производный класс|Базовый и производный класс]]** — объект наследника содержит базовую часть.
- **[[C++ inheritance#Порядок конструирования и уничтожения|Порядок конструирования]]** — сначала родитель, потом наследник; при уничтожении наоборот.
- **[[C++ inheritance#Тип наследования|Тип наследования]]** — `public`, `protected`, `private` наследование и обрезание видимости.
- **[[C++ inheritance#Указатель на базовый класс|Указатель на базовый класс]]** — почему `A *p = &b` работает.
- **[[C++ inheritance#Виртуальные функции|Виртуальные функции]]** — вызов метода наследника через интерфейс родителя и динамический полиморфизм.
- **[[C++ inheritance#Чисто виртуальные функции и абстрактные классы|Pure virtual и abstract classes]]** — `virtual f() = 0` и классы, объект которых нельзя создать.
- **[[C++ inheritance#Когда можно создать объект класса|Когда можно создать объект]]** — абстрактные классы, доступность конструкторов и корректное конструирование полей.
- **[[C++ inheritance#Как примерно работают виртуальные функции|vtable]]** — примерная механика виртуальных вызовов.
- **[[C++ inheritance#Виртуальный деструктор|Виртуальный деструктор]]** — зачем он нужен при удалении через базовый указатель.
- **[[C++ inheritance#Насколько глубоко можно наследоваться|Глубина наследования]]** — технически можно глубоко, но практически это быстро ухудшает код.
- **[[C++ inheritance#Множественное наследование|Множественное наследование]]** — несколько базовых классов и ромбовидная проблема.

### Другие возможности C++

- **[[C++ modern features#Namespace|Namespace]]** — пространства имен и `std`.
- **[[C++ modern features#`<math.h>` и `<cmath>`|Новые include-файлы]]** — C-заголовки и C++-обертки.
- **[[C++ modern features#`using namespace`|using namespace]]** — почему `using namespace std;` часто плохой тон.
- **[[C++ modern features#Анонимный namespace|Анонимный namespace]]** — локальные для файла имена.
- **[[C++ modern features#`auto`|auto]]** — выведение типа переменной.
- **[[C++ modern features#`decltype` и `decltype(auto)`|decltype]]** — точный тип выражения.
- **[[C++ modern features#Приведения типов в C++|C++-касты]]** — `static_cast`, `const_cast`, `reinterpret_cast`, `dynamic_cast`.
- **[[C++ modern features#Исключения|Исключения]]** — `throw`, `try`, `catch` и аварийное завершение при необработанном исключении.
- **[[C++ modern features#`noexcept`|noexcept]]** — обещание функции не бросать исключения.
- **[[C++ modern features#`std::optional`|std::optional]]** — значение либо есть, либо его нет.
- **[[C++ modern features#`std::expected`|std::expected]]** — результат либо ошибка без исключений.
- **[[C++ modern features#`enum` и `enum class`|enum и enum class]]** — обычные enum и строгие scoped enum.
- **[[C++ modern features#`inline`|inline]]** — встраивание и корректные определения функций в заголовках.
- **[[C++ modern features#Лямбда-функции|Лямбда-функции]]** — безымянные функции и список захвата.

### Шаблоны

- **[[C++ templates#Шаблоны функций|Шаблоны функций]]** — `template <typename T>` и порождение функций под конкретные типы.
- **[[C++ templates#Выведение типа шаблона|Выведение типа]]** — как компилятор понимает, чему равен `T`.
- **[[C++ templates#Шаблоны классов|Шаблоны классов]]** — `Box<int>` и `Box<double>` как разные типы.
- **[[C++ templates#Специализация|Специализация]]** — отдельная реализация шаблона для конкретного типа.
- **[[C++ templates#Почему шаблоны часто пишут в заголовках|Шаблоны в заголовках]]** — почему тело шаблона обычно должно быть видно компилятору.

### Стандартные контейнеры

- **[[C++ containers#Что такое контейнер|Контейнеры]]** — объекты стандартной библиотеки, которые хранят наборы других объектов.
- **[[C++ containers#`std::vector`|std::vector]]** — динамический массив из стандартной библиотеки.
- **[[C++ containers#Размер и вместимость|size и capacity]]** — текущий размер и выделенная память с запасом.
- **[[C++ containers#Доступ к элементам|Доступ к элементам]]** — `[]`, `at` и проверка границ.
- **[[C++ containers#Инициализация `vector`|Инициализация vector]]** — `{}`, `(...)` и важные различия.
- **[[C++ containers#`std::initializer_list`|std::initializer_list]]** — как функции и конструкторы принимают список `{...}`.
- **[[C++ containers#Важная опасность ссылки и указатели на элементы|Указатели и ссылки на элементы]]** — почему они могут стать висячими после перераспределения памяти.
- **[[C++ containers#`std::span`|std::span]]** — невладеющий вид на непрерывный участок памяти.
- **[[C++ containers#`std::unordered_map`|std::unordered_map]]** — хеш-таблица для пар ключ-значение.

### Владение и перемещение

- **[[C++ move and ownership#Зачем вообще нужно перемещение|Зачем нужно перемещение]]** — перенос владения вместо дорогого или неправильного копирования.
- **[[C++ move and ownership#`std::move`|std::move]]** — разрешение забирать ресурсы из объекта.
- **[[C++ move and ownership#Категории значений|Категории значений]]** — lvalue, prvalue, xvalue, glvalue и rvalue.
- **[[C++ move and ownership#Rvalue-ссылки|Rvalue-ссылки]]** — `T&&`, временные значения и перемещение.
- **[[C++ move and ownership#Конструктор перемещения|Конструктор перемещения]]** — `T(T&&)`, перенос ресурса и зачем там `noexcept`.
- **[[C++ move and ownership#Правило пяти|Правило пяти]]** — деструктор, копирование и две операции перемещения.
- **[[C++ move and ownership#`std::unique_ptr`|unique_ptr]]** — единоличное владение объектом.

### Qt

- **[[Qt basics#Общая архитектура Qt|Общая архитектура Qt]]** — C++ плюс метаобъектная система.
- **[[Qt basics#Event loop|Event loop]]** — цикл обработки событий в Qt-приложении.
- **[[Qt basics#`QObject`|QObject]]** — базовый класс Qt-объектов.
- **[[Qt basics#Parent/child владение|Parent/child]]** — дерево владения `QObject`.
- **[[Qt basics#Метаобъектная система|Метаобъектная система]]** — `moc` и дополнительный C++-код.
- **[[Qt basics#`Q_OBJECT`|Q_OBJECT]]** — макрос для метаобъектной информации.
- **[[Qt basics#Сигналы|Сигналы]]** — сообщения о событиях.
- **[[Qt basics#Слоты|Слоты]]** — функции, подключаемые к сигналам.
- **[[Qt basics#`connect`|connect]]** — связь сигнала и слота.
- **[[Qt basics#Важные макросы Qt|Важные макросы Qt]]** — `signals`, `slots`, `emit`, `Q_PROPERTY`.

--------------------------------------------------------------------------------
## Вопросы к экзамену

1. Общая структура программы на C/C++. Компиляция и сборка (линковка) программы. Статические и динамические библиотеки. `inline`.
2. Функции. Прототипы функций. Передача параметров и возврат значения. Рекурсия. Лямбда-функции.
3. Директивы препроцессора. Макроопределения. Защита заголовочных файлов от повторного подключения.
4. Время жизни и область видимости программных объектов, `namespace`.
5. Управляющие конструкции: операторы ветвления, выбора и организации циклов.
6. Целые и вещественные типы. Комплексные числа. Перечисляемый тип.
7. Указатели. Адресная арифметика. `restrict`. Ссылки.
8. Массивы. Взаимосвязь массивов и указателей. Многомерные массивы. VLA/VMT.
9. Неявные и явные преобразования типов.
10. Динамическое выделение памяти в C и C++.
11. Ввод/вывод в языке C (файловый и на консоль).
12. Перегрузка функций и операторов в C++. Параметры по умолчанию.
13. Структуры и объединения в C/C++. Классы в C++. Конструкторы и деструкторы. Специальные методы классов. `friend`, `explicit`.
14. Наследование. Разграничение доступа к элементам класса. Виртуальные функции.
15. Шаблоны.
16. Исключения в C++. Обработка ошибок через исключения и коды возврата. RAII.
17. Категории значений. Различия, использование, примеры.
18. `constexpr`, сравнение с `const`, `enum`, макросами. `volatile`. `typedef`, `using`. `auto`, `typeof`, `decltype`.

--------------------------------------------------------------------------------
